<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LocalHeroApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LocalHeroApp" parent="Base.Theme.LocalHeroApp" />
    
    <!-- No Action Bar theme for activities -->
    <style name="Theme.LocalHeroApp.NoActionBar" parent="Base.Theme.LocalHeroApp" />
</resources>